VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Optional: force auth email links to this origin (e.g. https://your-app.vercel.app).
# If unset, the current browser origin is used (right for most Vercel + local dev).
# VITE_SITE_URL=https://your-app.vercel.app
